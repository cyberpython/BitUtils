apply plugin: 'java'
apply plugin: 'eclipse'

jar {
    baseName = 'bitutils'
    version =  '0.0.1'
}

repositories {
    mavenCentral()
}

dependencies {
    testCompile("junit:junit:4.+")
}

task wrapper(type: Wrapper) {
    gradleVersion = '2.5'
}

